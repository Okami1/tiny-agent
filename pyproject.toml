[project]
name = "tiny-agent"
version = "0.1.0"
description = "A filesystem-and-fetch agent that answers questions about a directory"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=1.3.0",
    "python-dotenv>=1.2.3",
]

[tool.uv]
package = false
